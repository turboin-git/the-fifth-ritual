spring.datasource.url=jdbc:mysql://localhost:3306/fifth_ritual_db
spring.datasource.username=root
spring.datasource.password=root1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8080
jwt.secret=thefifthritual_super_secret_key_2024_make_this_long
jwt.expiration=86400000
# Session Management
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# Logging
logging.level.com.thefifthritual=DEBUG
logging.file.name=logs/fifth-ritual.log

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# App Info
spring.application.name=The Fifth Ritual
info.app.name=The Fifth Ritual
info.app.version=1.0.0


# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=smoothoperator561@gmail.com
spring.mail.password=utvedbyfpyzkrkva
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# App URL (for email links)
app.url=http://localhost:5173

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,loggers
management.endpoint.health.show-details=always

# Logging
logging.level.com.thefifthritual=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/fifth-ritual.log
logging.file.max-size=10MB
logging.file.max-history=30

# Khalti Payment Gateway
khalti.secret-key=8fe57ab9e5104df4a4055b80a079658c
khalti.base-url=https://dev.khalti.com/api/v2
khalti.return-url=http://localhost:5173/payment/callback
khalti.website-url=http://localhost:5173